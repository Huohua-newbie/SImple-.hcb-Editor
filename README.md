这次是纯手工写史山了）
基于摸鱼体验版，相关文件同理。理论上其余hcb可以同理操作，手动改function的offset即可。

(26-8-27更新)
加了一堆新功能，建议自行查看具体的定义判断如何使用。
msg的normal那边，现在发现实际上调用一次CG/black底图等即可，所以现在不强制重新显示，base那边删掉的音效也补回来了。

(26-9-12更新)
加入了新背景图导入的功能，一次只能导入一张，详细分支我懒得做了（）
一些背景图的入参个数不对的问题，这里单独加了个新的表完成。后续的话，得单独考虑一下怎么处理。
debug模式直接写入字节串和直接执行可用指令，这部分建议读过都有哪些可用指令再写。毕竟这个脚本基本上是自用，没多少异常情况预估。

(26-9-25更新)
加了一些和立绘有关的演出的内容，包括立绘的退场、移动等。
应该够用了，得找时间总结一下每条指令都可以怎么用。
